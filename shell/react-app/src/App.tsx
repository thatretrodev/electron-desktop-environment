import { useState } from 'react';
import { Button, ButtonGroup } from '@chakra-ui/react';

function App() {
	//const [count, setCount] = useState(0)

	return (
		<div style={{marginTop: 8, marginLeft: 8}}>
			<Button>Test</Button>
			{/*<Button style={{marginLeft: 8}}>Test</Button>*/}
		</div>
	);
}

export default App;
